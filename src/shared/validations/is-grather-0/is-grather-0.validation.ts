/**
 *  valida si un valor es positivo y mayor o igual a 0
 *
 * @param {number} value Valor a validar.
 * @return {boolean} Verdadero si el valor es positivo.
 */
export const IsGreaterThanZero = (value: number): boolean => {
  return value >= 0;
};
